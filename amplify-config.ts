/**
 * Amplify Configuration - Disabled for Single Sign-On
 * Mock configuration that doesn't initialize AWS services
 */

/**
 * Mock Amplify configuration
 * No actual AWS services are configured
 */
export const configureAmplify = () => {
  console.log('Amplify configuration skipped - using mock authentication');
};
